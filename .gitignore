# OS / editor
.DS_Store
Thumbs.db
.vscode/
*.log

# nunca secretos en el repo del sitio
.env
secrets/

# panel de fundador: datos en runtime (cuentas, snapshot, bandeja, token de sync) — jamás al repo
panel/data/

# solicitudes del formulario de auditoría (datos de prospectos) — jamás al repo
/data/

# comanda demo de velaslumin (sandbox): clave + pedidos en runtime — jamás al repo
sandbox/velaslumin/data/
sandbox/velaslumin/comanda/data/
